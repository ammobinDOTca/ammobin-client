{
  "name": "ammo-scrape",
  "private": true,
  "description": "",
  "dependencies": {
    "axios": "^0.15.2",
    "nuxt": "latest"
  },
  "scripts": {
    "dev": "nuxt",
    "build": "PROD=true nuxt build",
    "start": "PROD=true nuxt start",
    "generate": "PROD=true nuxt generate"
  }
}